# 项目 A 口径表（先定口径，再写 SQL）

> 规则：本表是 SQL、Python、README、简历四个位置的唯一口径来源。任何修改必须先改本表，
> 再同步改脚本并重跑关键数字。带“待核定”的项，在数据建库后按实际数据确认并记录日期范围。

| 指标 | 建议口径 | 备注 | 状态 |
|---|---|---|---|
| 统计范围 | 有完整订单记录、order_status 明确 | 数据时间窗：2016-09-04 ~ 2018-10-17（2016-09-04 21:15 下单） | 已核定 |
| GMV | Σ(price + freight_value)，剔除 order_status IN ('canceled','unavailable') | items 口径 R$15,735,527.03；payments 口径 R$15,739,137.01，差 R$3,609.98（0.02%），源于 301 单金额不一致 + 7 单有付款无 items + 1 单有 items 无付款 | 已核定 |
| 订单量 | 有 order_items 记录的有效订单数（98,199） | 不是 orders 表全部行数（99,441）；另 8 单有效但无 items | 已核定 |
| 客单价（AOV） | GMV ÷ 订单量 = R$160.24 | 如按客户计算需单独注明 | 已核定 |
| 复购率 | 全周期购买 ≥2 次（按 customer_unique_id）客户 ÷ 有购买客户 = 2,887/94,983 = 3.04% | 按去重有效订单，SQL6/7 已核验 | 已核定 |
| 首购月 Cohort 留存 | 用户首单所在月为 cohort；之后第 n 个月有任意订单的比例 | 第 0 个月 = 100%；第1月加权留存 0.45% | 已核定（SQL7） |
| RFM 阈值 | R：≤197天 记3、198–339天 记2、≥340天 记1；M：≤R$75.25 记1、R$75.26–152.09 记2、≥R$152.10 记3；F：复购=≥2 单（96.96% 客户仅 1 单） | 阈值记录在 sql/06 与 results/06_RFM分层_q2.csv，可重跑 | 已核定（SQL6） |
| 漏斗 | 订单状态序列：created→approved→invoiced→processing→shipped→delivered | 各层分母 = 上一层订单数；不与浏览级漏斗混用 | 建议口径 |
| 差评 | review_score ≤ 2 | 写在口径表里 | 固定 |
| 配送延迟 | order_delivered_customer_date > order_estimated_delivery_date | 只对已送达订单计算 | 固定 |

## 口径自测

> “我用了（什么数据、多少行/多少订单、什么时间窗），把（什么指标）定义为
> （分子/分母/剔除规则），发现（什么结论，量级多少），所以我建议（什么动作），
> 验证方式是（再拿什么数据/什么指标复核）。”
