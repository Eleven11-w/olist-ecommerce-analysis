# 数据字典（待建库时填写）

> 按手册 §2.2：下载全部 CSV 后，先用 Python 逐表检查列名、行数、重复主键，
> 再回填本表。order_id 在 order_items / order_payments / order_reviews 中是一对多，
> 先想清楚主键关系再导入。

| 文件（官方名） | 表名 | 行数（实测） | 主键/重复检查结果（已校验 2026-09-06） | 导入决定 |
|---|---|---|---|---|
| olist_orders_dataset.csv | orders | 99,441 | order_id 唯一，设主键 | 已导入 |
| olist_order_items_dataset.csv | order_items | 112,650 | (order_id, order_item_id) 唯一，设复合主键 | 已导入 |
| olist_order_payments_dataset.csv | order_payments | 103,886 | (order_id, payment_sequential) 唯一，设复合主键 | 已导入 |
| olist_order_reviews_dataset.csv | order_reviews | 99,224 | review_id 不唯一：去重后 98,410 个（重复 814 行/789 组）；改用自增 id 作主键 | 已导入 |
| olist_customers_dataset.csv | customers | 99,441 | customer_id 唯一，设主键；customer_unique_id 去重后 96,096（同一人可多地址） | 已导入 |
| olist_sellers_dataset.csv | sellers | 3,095 | seller_id 唯一，设主键 | 已导入 |
| olist_products_dataset.csv | products | 32,951 | product_id 唯一，设主键 | 已导入 |
| olist_geolocation_dataset.csv | geolocation | 1,000,163 | 无主键；同一 zip 前缀多行属正常，仅加索引 | 已导入 |
| product_category_name_translation.csv | product_category_name_translation | 71 | product_category_name 唯一，设主键 | 已导入 |

> 数据存放：`E:\03_Development\olist-data\`（原始 CSV 不入 GitHub；README 保留官方下载链接）。
> 导入细节：CSV 空串统一转 NULL；MySQL 导入时用 `ESCAPED BY ''` 保留评论文本中的反斜杠；
> reviews 与翻译表为 CRLF、其余 7 张为 LF。逐行核对脚本：`analysis/import_qc.py`。
